using App.Message.Util;

namespace App.Handler.Util
{

    public class ResKickHandler : Net.Handler {

        // 踢人
        public void Exec(Net.Message msg0)
        {
            ResKickMessage msg = (ResKickMessage)msg0;
            // todo
        }
    }
}