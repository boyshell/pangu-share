using App.Message.Skill;

namespace App.Handler.Skill
{

    public class ResSkillAddHandler : Net.Handler {

        // 添加技能
        public void Exec(Net.Message msg0)
        {
            ResSkillAddMessage msg = (ResSkillAddMessage)msg0;
            // todo
        }
    }
}