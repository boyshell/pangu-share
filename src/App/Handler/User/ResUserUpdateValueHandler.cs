using App.Message.User;

namespace App.Handler.User
{

    public class ResUserUpdateValueHandler : Net.Handler {

        // 更新值
        public void Exec(Net.Message msg0)
        {
            ResUserUpdateValueMessage msg = (ResUserUpdateValueMessage)msg0;
            // todo
        }
    }
}