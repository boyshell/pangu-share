using App.Message.Hero;

namespace App.Handler.Hero
{

    public class ResHeroAddSkillHandler : Net.Handler {

        // 装配技能
        public void Exec(Net.Message msg0)
        {
            ResHeroAddSkillMessage msg = (ResHeroAddSkillMessage)msg0;
            // todo
        }
    }
}