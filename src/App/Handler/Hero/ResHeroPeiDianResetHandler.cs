using App.Message.Hero;

namespace App.Handler.Hero
{

    public class ResHeroPeiDianResetHandler : Net.Handler {

        // 重置武将配点
        public void Exec(Net.Message msg0)
        {
            ResHeroPeiDianResetMessage msg = (ResHeroPeiDianResetMessage)msg0;
            // todo
        }
    }
}