using App.Message.Hero;

namespace App.Handler.Hero
{

    public class ResHeroChaiJieHandler : Net.Handler {

        // 拆解技能
        public void Exec(Net.Message msg0)
        {
            ResHeroChaiJieMessage msg = (ResHeroChaiJieMessage)msg0;
            // todo
        }
    }
}