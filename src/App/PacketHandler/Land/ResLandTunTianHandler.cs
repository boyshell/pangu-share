using App.PacketMessage.Land;

namespace App.PacketHandler.Land
{

    public class ResLandTunTianPacketHandler : Network.PacketHandler {

        // 屯田
        public void Exec(Network.PacketMessage msg0)
        {
            ResLandTunTianPacketMessage msg = (ResLandTunTianPacketMessage)msg0;
            // todo
        }
    }
}