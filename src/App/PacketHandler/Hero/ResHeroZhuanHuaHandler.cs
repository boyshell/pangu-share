using App.PacketMessage.Hero;

namespace App.PacketHandler.Hero
{

    public class ResHeroZhuanHuaPacketHandler : Network.PacketHandler {

        // 武将转化
        public void Exec(Network.PacketMessage msg0)
        {
            ResHeroZhuanHuaPacketMessage msg = (ResHeroZhuanHuaPacketMessage)msg0;
            // todo
        }
    }
}